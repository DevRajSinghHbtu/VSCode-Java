class Solution {
    public boolean check(int[] nums) {
        int l = nums.length;
        int i = 0;
        boolean check = false;
       //  int peak =  peakIndexInMountainArray(nums);
        while(i<l)
        {
            if(nums[i]<nums[i+1])
            {
                i++;
                
            }
         

        }
      
    }
}
